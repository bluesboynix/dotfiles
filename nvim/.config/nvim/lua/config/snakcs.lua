require("snacks").setup({
  git = { blame = true },     -- optional subset
  noice = { enabled = false }, -- we won't enable noice here
  fancy = { enabled = true }, -- small animations (optional)
})
