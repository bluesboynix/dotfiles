conky.config = {
    own_window = true,
    own_window_type = 'dock',
    own_window_hints = 'undecorated,below,sticky,skip_taskbar,skip_pager',
    own_window_argb_visual = true,
    own_window_argb_value = 0,
    own_window_transparent = true,

    out_to_x = false,
    out_to_wayland = true,
    double_buffer = true,
    update_interval = 600,  -- weather doesn’t need frequent updates

    minimum_width = 250,
    maximum_width = 250,
    minimum_height = 100,
    alignment = 'top_left',   -- center top
    gap_x = 30,
    gap_y = 400,

    use_xft = true,
    font = 'Fira Code:size=12',
    default_color = '#2fffff',

    draw_borders = true,
    border_width = 2,
    border_inner_margin = 12,

    background = false,
};

conky.text = [[
${alignc}${font Fira Code:bold:size=10}WEATHER$font

${font Fira Code:size=13}
${alignc}${execi 600 curl -s 'wttr.in/Delhi?format=1'}
$font
]];
